float tabelSepatu(float totalPanjang, String tipeNeg){
  // Ukuran Amerika  
  if(tipeNeg == "U" && totalPanjang >= 23.50){
      ukuranSepatu  = 7;
  }
  if(tipeNeg == "U" && totalPanjang >= 23.80 ){
      ukuranSepatu  = 7.5;
  }
  if(tipeNeg == "U" && totalPanjang >= 24.10 ){
      ukuranSepatu  = 8;
  }
  if(tipeNeg == "U" && totalPanjang >= 24.60 ){
      ukuranSepatu  = 8.5;
  }
  if(tipeNeg == "U" && totalPanjang >= 25.10 ){
      ukuranSepatu  = 9;
  }
  if(tipeNeg == "U" && totalPanjang >= 25.40 ){
      ukuranSepatu  = 9.5;
  }
  if(tipeNeg == "U" && totalPanjang >= 25.90 ){
      ukuranSepatu  = 10;
  }
  if(tipeNeg == "U" && totalPanjang >= 26.20 ){
      ukuranSepatu  = 10.5;
  }
  if(tipeNeg == "U" && totalPanjang >= 26.70 ){
      ukuranSepatu  = 11;
  }
  if(tipeNeg == "U" && totalPanjang >= 27.10 ){
      ukuranSepatu  = 11.5;  
  }
  if(tipeNeg == "U" && totalPanjang >= 27.60 ){
      ukuranSepatu  = 12;  
  }
  if(tipeNeg == "U" && totalPanjang >= 28.30 ){
      ukuranSepatu  = 12.5;
  }
  if(tipeNeg == "U" && totalPanjang >= 28.60 ){
      ukuranSepatu  = 13;
  }
  if(tipeNeg == "U" && totalPanjang >= 29.40 ){
      ukuranSepatu  = 13.5;
      
  }
  
  // Ukuran Inggris
  if(tipeNeg == "E" && totalPanjang >= 23.50){
      ukuranSepatu  = 36;
  }
  if(tipeNeg == "E" && totalPanjang >= 23.80 ){
      ukuranSepatu  = 37;
  }
  if(tipeNeg == "E" && totalPanjang >= 24.10 ){
      ukuranSepatu  = 37.5;
  }
  if(tipeNeg == "E" && totalPanjang >= 24.60 ){
      ukuranSepatu  = 38;
  }
  if(tipeNeg == "E" && totalPanjang >= 25.10 ){
      ukuranSepatu  = 38.5;
  }
  if(tipeNeg == "E" && totalPanjang >= 25.40 ){
      ukuranSepatu  = 39;
  }
  if(tipeNeg == "E" && totalPanjang >= 25.90 ){
      ukuranSepatu  = 40;
  }
  if(tipeNeg == "E" && totalPanjang >= 26.20 ){
      ukuranSepatu  = 41;
  }
  if(tipeNeg == "E" && totalPanjang >= 26.70 ){
      ukuranSepatu  = 42;
  }
  if(tipeNeg == "E" && totalPanjang >= 27.10 ){
      ukuranSepatu  = 43;  
  }
  if(tipeNeg == "E" && totalPanjang >= 27.60 ){
      ukuranSepatu  = 44;  
  }
  if(tipeNeg == "E" && totalPanjang >= 28.30 ){
      ukuranSepatu  = 45;
  }
  if(tipeNeg == "E" && totalPanjang >= 28.60 ){
      ukuranSepatu  = 46;
  }
  if(tipeNeg == "E" && totalPanjang >= 29.40 ){
      ukuranSepatu  = 47;
      
  }

  // Ukuran Jepang
  if(tipeNeg == "J" && totalPanjang >= 23.50){
      ukuranSepatu  = 22;
  }
  if(tipeNeg == "J" && totalPanjang >= 23.80 ){
      ukuranSepatu  = 22.5;
  }
  if(tipeNeg == "J" && totalPanjang >= 24.10 ){
      ukuranSepatu  = 23;
  }
  if(tipeNeg == "J" && totalPanjang >= 24.50 ){
      ukuranSepatu  = 23.5;
  }
  if(tipeNeg == "J" && totalPanjang >= 24.80 ){
      ukuranSepatu  = 24;
  }
  if(tipeNeg == "J" && totalPanjang >= 25.10 ){
      ukuranSepatu  = 24.5;
  }
  if(tipeNeg == "J" && totalPanjang >= 25.40 ){
      ukuranSepatu  = 25;
  }
  if(tipeNeg == "J" && totalPanjang >= 25.70 ){
      ukuranSepatu  = 25.5;
  }
  if(tipeNeg == "J" && totalPanjang >= 26.00 ){
      ukuranSepatu  = 26;
  }
  if(tipeNeg == "J" && totalPanjang >= 26.70 ){
      ukuranSepatu  = 27;
  }
  if(tipeNeg == "J" && totalPanjang >= 27.30 ){
      ukuranSepatu  = 28;  
  }
  if(tipeNeg == "J" && totalPanjang >= 28.00 ){
      ukuranSepatu  = 29;
  }
  if(tipeNeg == "J" && totalPanjang >= 28.60 ){
      ukuranSepatu  = 30;
  }
  if(tipeNeg == "J" && totalPanjang >= 29.20 ){
      ukuranSepatu  = 31;
  }

  // Ukuran Indonesia
  if(tipeNeg == "I" && totalPanjang >= 23.50){
      ukuranSepatu  = 6.5;
  }
  if(tipeNeg == "I" && totalPanjang >= 23.80 ){
      ukuranSepatu  = 7;
  }
  if(tipeNeg == "I" && totalPanjang >= 24.10 ){
      ukuranSepatu  = 7.5;
  }
  if(tipeNeg == "I" && totalPanjang >= 24.60 ){
      ukuranSepatu  = 8;
  }
  if(tipeNeg == "I" && totalPanjang >= 25.10 ){
      ukuranSepatu  = 8.5;
  }
  if(tipeNeg == "I" && totalPanjang >= 25.40 ){
      ukuranSepatu  = 9;
  }
  if(tipeNeg == "I" && totalPanjang >= 25.90 ){
      ukuranSepatu  = 9.5;
  }
  if(tipeNeg == "I" && totalPanjang >= 26.20 ){
      ukuranSepatu  = 10;
  }
  if(tipeNeg == "I" && totalPanjang >= 26.70 ){
      ukuranSepatu  = 10.5;
  }
  if(tipeNeg == "I" && totalPanjang >= 27.10 ){
      ukuranSepatu  = 11;  
  }
  if(tipeNeg == "I" && totalPanjang >= 27.60 ){
      ukuranSepatu  = 11.5;  
  }
  if(tipeNeg == "I" && totalPanjang >= 28.30 ){
      ukuranSepatu  = 12;
  }
  if(tipeNeg == "I" && totalPanjang >= 28.60 ){
      ukuranSepatu  = 12.5;
  }
  if(tipeNeg == "I" && totalPanjang >= 29.40 ){
      ukuranSepatu  = 13;
      
  }

  return ukuranSepatu;
}
